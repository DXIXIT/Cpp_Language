#include <iostream>

using namespace std;

int main()
{
	int val1 = 12;
	int val2 = 15;

	bool expression = true;
	//Ternary operator is a shorthand way of writing if-else
	
	int x = (expression) ? val1: val2;

	//If expression is true, return val1, else return val 2.
}